# Append the tone-check button to the Minimal CKEditor 5 toolbar.
# config_rewrite merges sequential lists by appending, so this adds the button
# to the end of the existing toolbar without redefining it.
settings:
  toolbar:
    items:
      - helfiAiToneCheck
